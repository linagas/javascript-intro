console.log('index')

let firstName;
let lastName = 'Inagas';

console.log(`${firstName || 'No firstName'} ${lastName || 'No lastName'}`);

const isActive = true

const message = isActive ? 'yes' : 'no'

console.log(message);
